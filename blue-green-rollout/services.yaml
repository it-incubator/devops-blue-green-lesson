apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo-active
  namespace: lessons
spec:
  ports:
    - port: 4565
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: rollouts-demo

---
apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo-preview
  namespace: lessons
spec:
  ports:
    - port: 4565
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: rollouts-demo
